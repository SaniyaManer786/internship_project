# Dmless - Recruiter Hiring Platform

Recruiters create jobs with MCQ knockout questions. Candidates apply via shareable links.

## Setup
1. cd backend && npm install
2. Update backend/.env with MongoDB URI
3. cd backend && npm start
4. Open http://localhost:3000
